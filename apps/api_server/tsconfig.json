{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "ignoreDeprecations": "6.0"
  },
  "include": [
    "../../shared/**/*.ts",
    "../../middleware/**/*.ts",
    "../../types/**/*.ts",
    "./**/*.ts"
  ],
  "exclude": ["node_modules", "dist"]
}
