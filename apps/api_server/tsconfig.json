{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "outDir": "./dist"
  },
  "include": [
    "../../shared/**/*.ts",
    "../../middleware/**/*.ts",
    "../../types/**/*.ts",
    "./**/*.ts"
  ],
  "exclude": ["node_modules", "dist"]
}
