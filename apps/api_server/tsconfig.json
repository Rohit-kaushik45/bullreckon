{
  "extends": "../../tsconfig.json",
  "include": [
    "../../shared/**/*.ts",
    "../../middleware/**/*.ts",
    "../../types/**/*.ts",
    "./**/*.ts"
  ],
  "exclude": ["node_modules", "dist"]
}
